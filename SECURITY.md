# Security

- 默认监听 127.0.0.1。Docker 端口映射到宿主 127.0.0.1。
- 模型 Key 与 NCBI Key 仅存在于进程内存（及用户本机环境变量）。不要写入数据库、日志、Excel、JSON 审计包或 URL。
- 报告漏洞请不要公开贴出含 Key 的日志。维护者联系方式发布前填入此处。
- 自定义模型 Base URL 与 GEO 下载入口隔离；GEO 抓取仅允许 NCBI 官方域名。
- 本仓库第一版是单用户本地工具，不能直接暴露到公网。
